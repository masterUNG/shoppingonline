import 'package:flutter/material.dart';

class OrderShop extends StatelessWidget {
  const OrderShop({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Text('Order'),);
  }
}